import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  width: 100%;
  background: white;
  justify-content: space-between;
  padding: 1rem;

  ul {
    display: flex;
    gap: 1rem;
  }

  .Container-icon {
    display: flex;
    gap: 1rem;
  }
`;
